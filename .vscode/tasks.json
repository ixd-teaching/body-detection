{
   // See https://go.microsoft.com/fwlink/?LinkId=733558
   // for the documentation about the tasks.json format
   "version": "2.0.0",
   "tasks": [
      {
         "label": "Open Chrome in Debug mode",
         "type": "shell",
         "command": "open -a \"Google Chrome\" --args --remote-debugging-port=9222 --user-data-dir=remote-debug-profile" 
      }
   ]
}