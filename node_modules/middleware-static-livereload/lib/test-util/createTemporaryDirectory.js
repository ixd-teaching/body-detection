"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createTemporaryDirectory = void 0;
const os = require("os");
const fs = require("fs");
const path = require("path");
const createTemporaryDirectory = (prefix = 'temp-') => fs.mkdtempSync(path.join(os.tmpdir(), prefix));
exports.createTemporaryDirectory = createTemporaryDirectory;
