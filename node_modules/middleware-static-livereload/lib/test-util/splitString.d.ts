export interface SplitResult {
    value: string;
    start: number;
    end: number;
    next: number;
}
export declare const splitString: (input: string, separator: string) => Generator<SplitResult>;
