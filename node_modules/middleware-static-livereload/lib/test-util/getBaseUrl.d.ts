import { URL } from 'url';
import { AddressInfo, Server } from 'net';
export declare const getBaseUrl: (addressInfo: string | AddressInfo | null, pathname?: string) => URL;
export declare const getBaseUrlForServer: (server: Server, pathname: string) => URL;
