"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getFile = void 0;
const path = require("path");
const statOrNull_1 = require("./statOrNull");
const LibError_1 = require("./LibError");
const getFile = async (absolutePath, baseDirectory = process.cwd()) => {
    const relativePath = path.relative(baseDirectory, absolutePath);
    const stats = await statOrNull_1.statOrNull(absolutePath);
    if (stats && stats.isFile()) {
        return {
            path: absolutePath,
            relativePath,
            stats,
        };
    }
    throw new LibError_1.LibError('ENOENT', relativePath);
};
exports.getFile = getFile;
