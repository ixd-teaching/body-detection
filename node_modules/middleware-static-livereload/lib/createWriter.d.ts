/// <reference types="node" />
import * as util from 'util';
import { ILog } from './types';
export declare const createWriter: (stream: NodeJS.WritableStream, inspectOptions: util.InspectOptions) => ILog;
